package at.manuelsch.LinkScraper;

/**
 * Fetches a given URL, extracts a set of links that point to the same domain and executes the given fallbacks
 */
public interface ScrapeWorker extends Runnable {

}
